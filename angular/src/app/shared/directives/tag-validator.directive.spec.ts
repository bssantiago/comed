import { TagValidatorDirective } from './tag-validator.directive';

describe('TagValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new TagValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
